writer:
  logdir: runs/cnn_dnn2/step_lr/lz2_lxy256

run: optimizer

data:
  dataConfigPath: datasets/data_config.yaml
  trainDatasets:
    - lz2_lxy256
  batchSize: 8

training:
  epochs: 20000
  eval_epochs: 200
  model:
    name: cnn_dnn2
  loss: mse
  optimizer:
    name: adam
    lr: 0.001
    betas: 0.9, 0.999
  lrScheduler:
    name: step
    stepStepSize: 5000
    stepGamma: 0.5
